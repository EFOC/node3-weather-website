<!DOCTYPE html>

<html>
    <head>
        <title>404</title>
        <link rel="icon" href="/img/weather.png" />
        <link rel="stylesheet" href="/css/styles.css" />
    </head>
    
    <body>
        <div class="main-content">
            <h2>{{errorMessage}}</h2>
            {{>header}}
        </div>
        {{>footer}}
    </body>
</html>